#include "lists.h"

/**
 * reverse_listint - reverse linked list
 * @head: head list
 * Code by - yusifhuseini
 * Return: first node
 */
listint_t *reverse_listint(listint_t **head)
{
	listint_t *p;
	listint_t *n;

	p = NULL;
	n = NULL;

	while (*head != NULL)
	{
		n = (*head)->next;
		(*head)->next = p;
		p = *head;
		*head = n;
	}

	*head = p;
	return (*head);
}
